# ย้าย Central ไป IP ใหม่ โดยไม่ต้องลง agent ใหม่ทุกเครื่อง

เดิม: central เปลี่ยน IP = ต้องเดินไปลง agent ใหม่ทีละเครื่อง (ที่อยู่ central ฝังอยู่ทั้งใน
`agent_config.json`, `/etc/filebeat/filebeat.yml` และ rule ขาออกของ ufw)

ตอนนี้: **ทำทุกอย่างจบใน `setup-server.sh`** — agent จำที่อยู่ได้หลายที่และย้ายตามเอง

---

## วิธีใช้

รัน `sudo ./setup-server.sh` แล้วตอบที่อยู่ (หรือพอร์ต) ใหม่ตามปกติ **ตอนที่อยู่เดิมยังใช้งานได้อยู่**
สคริปต์ไม่ถามอะไรเพิ่ม — มันดูเองว่าเครื่องนี้มีที่อยู่ใหม่แล้วหรือยัง

| สภาพเครื่อง | สคริปต์ทำ |
|---|---|
| ที่อยู่ใหม่อยู่บนเครื่องนี้แล้ว (เช่นย้ายไปอีกขา) | ประกาศให้ agent · ย้ายได้ทันที ไม่ต้องทำอะไรต่อ |
| ที่อยู่ใหม่ยังไม่มีบนเครื่อง | ประกาศให้ agent **ก่อน** แล้วบอกวิธีตั้ง IP ให้ท้ายสคริปต์ |
| เปลี่ยนแค่พอร์ต | ประกาศอย่างเดียว |
| ยังไม่มี agent ในระบบ | ข้ามทั้งหมด |

**สคริปต์ไม่แตะการตั้งค่าเครือข่ายของเครื่องเลย** — งานนั้นเป็นของแอดมิน (พลาดแล้วเครื่องหลุด
จากเน็ตถาวร กู้ยากกว่าเดิมมาก) ท้ายสคริปต์จะพิมพ์ไฟล์ netplan ที่ต้องสร้างให้ครบพร้อมค่าจริง

```
  [!] This machine does not hold 192.168.56.170 - its own services cannot reach Redis until it does
       (the agents are fine: they keep using 192.168.56.119 and switch by themselves
       about half a minute after it stops answering)

       Give this machine the address, permanently:

         sudo tee /etc/netplan/60-securelog.yaml >/dev/null <<'EOF'
         network:
           version: 2
           ethernets:
             enp0s10:
               dhcp4: false
               addresses: [192.168.56.170/24]
         EOF
         sudo chmod 600 /etc/netplan/60-securelog.yaml
         sudo netplan try            # ถอยกลับเองถ้าเน็ตหลุด ปลอดภัยกว่า netplan apply

       (a new file - do not edit 50-cloud-init.yaml, cloud-init overwrites it)
       Services here reconnect on their own within seconds - nothing to restart.
```

### ลำดับที่ถูกต้อง

```
1. sudo ./setup-server.sh   ตอบที่อยู่ใหม่        <- ตอนที่อยู่เดิมยังใช้ได้
2. ดูตารางท้ายสคริปต์ว่า agent รับที่อยู่ใหม่แล้ว
3. ตั้ง IP ตามที่สคริปต์บอก (netplan try)
4. ที่อยู่เดิมหาย -> agent ย้ายเองใน ~35 วิ · service ของ central ฟื้นเองทันทีที่ IP มา
```

**ห้ามทำสลับลำดับ** — ถ้าเปลี่ยน IP ก่อนรัน setup ช่องทางคุยกับ agent จะตายไปพร้อมที่อยู่เดิม
ประกาศไม่ถึงใครเลย แล้วต้องเดินไปลง agent ใหม่ทุกเครื่อง

### ที่อยู่ที่ "มีอยู่แต่ไม่ถาวร"

ถ้าที่อยู่ใหม่ถูกแปะไว้ด้วย `ip addr add` เฉย ๆ (ไม่ได้อยู่ใน netplan และไม่ได้มาจาก DHCP)
สคริปต์จะเตือนว่าหายแน่ตอน reboot
```
  [!] 192.168.56.160 is on enp0s10 but only until the next reboot (nothing in /etc/netplan holds it)
```
> กันเคสที่น่ากลัวที่สุด: ย้ายเสร็จ ทุกอย่างดูดี แล้วอีก 3 เดือน reboot ทีเดียวระบบล่ม
> โดยไม่มีใครนึกออกว่าทำไม

## ประกาศ 2 รอบ ทำไม

```
เวลา ────────────────────────────────────────────────────────►
      [.env/cert เขียนใหม่]     [Redis restart]    [service ขึ้นครบ]
             │                        │                   │
        รอบ 1 ┘                       │                   └ รอบ 2
   ยิงผ่านที่อยู่+พอร์ต "เดิม"           สายเดิมตายตรงนี้       ที่อยู่ใหม่พร้อมจริง
   agent ยังเกาะอยู่ = ได้ยินแน่                             agent ย้ายทันที
```

รอบ 1 คือรอบที่ทำให้ **เปลี่ยนพอร์ตแล้ว agent ยังตามได้** เพราะพอ Redis ย้ายพอร์ตแล้ว
agent จะต่อไม่ได้อีกเลยและไม่มีทางได้ยินอะไรอีก

cert ในรอบที่ย้าย IP ถูกออกให้ครอบ **ทั้งที่อยู่ใหม่และเก่า** เพื่อไม่ให้ agent ที่ยังเกาะที่อยู่เดิม
ถูกตัดกลางคันตอน Redis restart ด้วย cert ใบใหม่

## ตารางท้ายสคริปต์

```
  AGENT                STATUS    CENTRAL IT IS ATTACHED TO
  Agent-01             online    192.168.56.150:6380
  1 of 1 online
```

ค่านี้ agent รายงานเข้ามาเองทุกวินาที (ไม่ใช่อ่านจากไฟล์ config) จึงใช้ยืนยันได้ว่าย้ายครบจริง
สคริปต์จะรอให้ agent ย้ายเสร็จก่อนพิมพ์ตาราง (ไม่เกิน 25 วินาที) และแสดงตารางนี้ทุกครั้งที่รัน
แม้ไม่ได้ย้ายอะไร

## เผื่อไว้ตั้งแต่ตอนติดตั้ง (แนะนำ)

ใส่ที่อยู่สำรองล่วงหน้า agent จะหา central เจอแม้ตอนย้ายมันจะปิดอยู่และไม่ได้รับประกาศเลย

- ฝั่ง central: `AGENT_CENTRAL_CANDIDATES="192.168.56.150 central.local"` ใน `.env`
  (setup-server.sh ใส่ที่อยู่พวกนี้ลง SAN ของ cert ให้ด้วย และฝังไปกับทุก package ที่ออกใหม่)
- ฝั่ง agent: `CENTRAL_CANDIDATES="..."` ใน `site.conf` แล้ว `sudo ./setup.sh`

ลง agent ซ้ำไม่ทำให้ลืมที่อยู่ที่เคยรู้ — setup.sh รวมที่อยู่เก่าใน `agent_config.json`
เข้ากับที่อยู่ใหม่ใน site.conf ให้เอง

## ทำไมถึงหลอกให้ย้ายไปเครื่องปลอมไม่ได้

agent จะยอมย้ายก็ต่อเมื่อ **ผ่านครบทุกด่าน**

1. คำสั่งจ่าหน้าถึง agent_id ตัวนี้
2. ลายเซ็น HMAC-SHA256 ตรง โดยกุญแจคือ `sha256(secret token ของ agent ตัวนั้น)` —
   ฝั่ง central มีค่านี้อยู่แล้วในคอลัมน์ `secret_token_hash` จึงไม่ต้องเก็บ token ตัวจริงไว้เซ็น
3. คำสั่งออกมาไม่เกิน 15 นาที และ nonce ยังไม่เคยถูกใช้ (กันเอาข้อความเก่ามายิงซ้ำ)
4. **ที่อยู่ใหม่ต้องคุยแบบ mTLS ผ่านจริง** — cert ฝั่งเซิร์ฟเวอร์ต้องออกโดย CA ใบเดิม
   ครอบ IP นั้น และรับ client cert ของ agent + รหัส ACL เดิม แล้ว `PING` ตอบ

ด่านที่ 4 คือด่านที่สำคัญที่สุด: ต่อให้ใครแอบส่งคำสั่งเข้ามาได้ ก็ต้องมีกุญแจของ CA
ถึงจะยกเครื่องปลอมขึ้นมารับ agent ได้

## เวลาที่ใช้จริง (วัดบนเครื่องทดสอบ)

| สถานการณ์ | เวลาตั้งแต่เหตุเกิดจนย้ายเสร็จ |
|---|---|
| ที่อยู่ใหม่ใช้ได้แล้วตอนประกาศ (ข้อ 1 / หลายขา) | ~2 วินาที |
| ที่อยู่เดิมล่ม แล้วไล่หาที่อยู่สำรอง | ~35 วินาที |
| agent เพิ่งเปิดเครื่องมาเจอที่อยู่เดิมล่ม | ~27 วินาที |
| รัน setup-server.sh เปลี่ยน IP (ออก cert ใหม่ด้วย) | ~35 วินาที นับจากสคริปต์เริ่มแก้ของ |
| รัน setup-server.sh เปลี่ยนพอร์ต Redis | ~70 วินาที (ต้องรอสายเดิมล่มก่อน) |

## ข้อจำกัดที่ยังเหลือ

- agent ที่ **ปิดอยู่ตอนประกาศ และไม่มีที่อยู่สำรองติดตัวเลย** จะหา central ไม่เจอ
  → ป้องกันด้วย `AGENT_CENTRAL_CANDIDATES` ตั้งแต่ตอนออก package
- ถ้า **CA เปลี่ยนใบ** (`setup-server.sh` สร้าง Root CA ใหม่) agent เดิมใช้ต่อไม่ได้อยู่ดี
  ต้องลงใหม่ — ให้ backup โฟลเดอร์ `cert/central/` ไปด้วยเสมอเวลาย้ายเครื่อง
- **รหัส Redis ของบัญชี agent** ถ้าเปลี่ยน ก็ต้องลง agent ใหม่เหมือนเดิม (คนละเรื่องกับ IP)
- IP ของ **ตัว agent เอง** ยังผูกกับ agent_id ที่ central อยู่ ถ้าเครื่อง agent ย้าย IP
  ยังต้องใช้ "Regenerate Download Link" แล้วลงใหม่ตามเดิม
